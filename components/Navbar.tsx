"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { useState } from "react";
const links = [{ href: "/", label: "home" }, { href: "/about", label: "about" }, { href: "/experience", label: "experience" }, { href: "/skills", label: "skills" }, { href: "/projects", label: "projects" }, { href: "/certifications", label: "certs" }, { href: "/resume", label: "resume" }, { href: "/contact", label: "contact" }];
export default function Navbar() { const pathname = usePathname(); const [open, setOpen] = useState(false); return <nav className="sticky top-0 z-30 border-b border-term-border bg-term-bg/90 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between gap-4 px-4 py-3"><Link href="/" className="font-semibold text-green">$ <span className="text-cyan">aatif@portfolio</span>:~</Link><div className="hidden items-center gap-4 lg:flex">{links.map((link) => <Link key={link.href} href={link.href} className={pathname === link.href ? "nav-active" : "nav-link"}>{link.label}</Link>)}</div><button className="rounded border border-term-border px-2 py-1 text-sm text-green lg:hidden" onClick={() => setOpen(!open)} aria-expanded={open} aria-label="Toggle navigation">{open ? "×" : "☰"}</button></div>{open && <div className="border-t border-term-border bg-term-bg px-4 py-4 lg:hidden"><div className="grid grid-cols-2 gap-3">{links.map((link) => <Link key={link.href} onClick={() => setOpen(false)} href={link.href} className={pathname === link.href ? "nav-active" : "nav-link"}>{link.label}</Link>)}</div></div>}</nav>; }
