import Link from "next/link";
import type { Project } from "@/data/projects";
export default function ProjectCard({ project }: { project: Project }) { return <article className="terminal-card flex h-full flex-col p-5"><p className="terminal-label">{project.type}</p><h2 className="mt-2 text-xl text-green">{project.name}</h2><p className="mt-3 text-sm text-term-fg">{project.description}</p><div className="mt-4 space-y-2 text-xs"><p><span className="text-cyan">Problem:</span> {project.problem}</p><p><span className="text-cyan">Built:</span> {project.built}</p></div><div className="mt-4 flex flex-wrap gap-2">{project.tags.map((tag) => <span className="skill-chip" key={tag}>{tag}</span>)}</div><div className="mt-auto flex flex-wrap gap-4 pt-5 text-sm"><Link className="terminal-link" href={`/projects/${project.slug}`}>$ cat case-study.md</Link>{project.github && <Link className="text-cyan hover:text-green" href={project.github} target="_blank">GitHub ↗</Link>}</div></article>; }
