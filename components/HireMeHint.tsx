export default function HireMeHint() {
  return (
    <p className="text-center text-xs text-term-fg/60">
      tip: type <code className="text-cyan">sudo hire-me</code> anywhere on the
      site
    </p>
  );
}
