import Link from "next/link";
import type { Project } from "@/data/projects";
export default function ProjectCaseStudy({ project }: { project: Project }) { const sections = [["Overview", project.description], ["Problem", project.problem], ["Architecture", project.architecture], ["Implementation", project.built], ...(project.databaseDesign ? [["Database Design", project.databaseDesign]] : [])] as [string, string][]; return <div className="space-y-5">{sections.map(([title, text]) => <section key={title}><h2 className="text-lg text-green">## {title}</h2><p className="mt-2 text-sm text-term-fg">{text}</p></section>)}<section><h2 className="text-lg text-green">## Engineering Decisions</h2><ul className="mt-2 space-y-2 text-sm text-term-fg">{project.highlights.map((item) => <li key={item}>- {item}</li>)}</ul></section><section><h2 className="text-lg text-green">## Challenges</h2><p className="mt-2 text-sm text-term-fg">{project.challenges.join(" ")}</p></section><section><h2 className="text-lg text-green">## What I Learned</h2><p className="mt-2 text-sm text-term-fg">{project.learned.join(" ")}</p></section><section><h2 className="text-lg text-green">## Tech Stack</h2><div className="mt-3 flex flex-wrap gap-2">{project.tags.map((tag) => <span className="skill-chip" key={tag}>{tag}</span>)}</div></section><div className="border-t border-term-border pt-4 text-sm"><p className="text-amber">$ git remote -v</p><div className="mt-2 flex flex-wrap gap-4">{project.github && <Link className="terminal-link" href={project.github} target="_blank">GitHub ↗</Link>}{project.demo && <Link className="terminal-link" href={project.demo} target="_blank">Live Demo ↗</Link>}</div></div></div>; }
