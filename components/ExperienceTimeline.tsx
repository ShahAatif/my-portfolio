import { experience } from "@/data/experience";
export default function ExperienceTimeline({ compact = false }: { compact?: boolean }) { const rows = compact ? experience.slice(0, 2) : experience; return <div className="space-y-5 border-l border-term-cyan/60 pl-5">{rows.map((job) => <article className="relative" key={job.company}><span className="absolute -left-[30px] top-1.5 h-2.5 w-2.5 rounded-full bg-term-green shadow-[0_0_12px_#00ff41]" /><div className="flex flex-wrap items-baseline justify-between gap-2"><div><p className="text-green">{job.role}</p><p className="text-sm text-cyan">{job.company} · {job.location}</p></div><span className="text-xs text-amber">{job.period}</span></div><p className="mt-2 text-sm text-term-fg">{job.summary}</p>{!compact && <><ul className="mt-3 space-y-1 text-sm text-term-fg">{job.responsibilities.map((item) => <li key={item}><span className="mr-2 text-cyan">›</span>{item}</li>)}</ul><div className="mt-3 flex flex-wrap gap-2">{job.technologies.map((tech) => <span className="skill-chip" key={tech}>{tech}</span>)}</div></>}</article>)}</div>; }
